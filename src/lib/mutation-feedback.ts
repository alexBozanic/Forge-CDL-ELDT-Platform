export type MutationState = { error?: string; success?: string };
export const mutationFailure =
  "We could not confirm that the change was saved. Your entries remain here. Check the current record before trying again; no automatic retry was made.";
